const { marca, product } = require("../models/relacion");

module.exports = {
    async addMarca(req,res){
        try {
            
        } catch (error) {
            
        }
    }
}